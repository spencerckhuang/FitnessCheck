import { View, StyleSheet, Text } from 'react-native';
import React from 'react';

const WorkoutType = ( {GlobalState} ) => { 

    return (
        <View>
            <Text>This would be a workout</Text>
        </View>
    )
}

const styles=StyleSheet.create({

});


export default WorkoutType;